import type { EmailConfig } from "./src/types";

export default {
  copyright: {
    owner: "@org",
    year: new Date().getFullYear(),
  },
  defaults: {
    logoUrl: undefined,
    heroImageUrl: undefined,
  },
} as EmailConfig;
