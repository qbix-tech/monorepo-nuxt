import { defineSeedConfig } from "./src/module/seed/definer";

export default defineSeedConfig({
  seed: {
    jobs: {
      // add seed data jobs here
    },
    tests: {
      // add seed test data jobs here
    },
  },
});
